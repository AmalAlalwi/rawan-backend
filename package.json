{
  "name": "json-server-backend",
  "version": "1.0.0",
  "description": "json-server backend for deployment on Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
